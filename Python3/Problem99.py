import time
import math

def compare_exponentials():
    pass

start = time.time()
answer = compare_exponentials()
elapsed = (time.time() - start)

print("Found " + str(answer) + " in " + str(elapsed) + " seconds.")
