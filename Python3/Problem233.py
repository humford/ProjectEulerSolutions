import math


def f(N):
    c = circle(N)
    pass
    # (x - c[0][0]) ** 2 + (y - c[0][1]) ** 2


def circle(N):
    center = (math.sqrt(N), math.sqrt(N))
    # radius = math.sqrt
    return center

for x in range(1, 10 ** 11):
    pass
