import math

def f(N):
	c = circle(N)
	(x - c[0][0]) ** 2 + (y - c[0][1]) ** 2 

def circle(N):
	center = (math.sqrt(N), math.sqrt(N))
	radius = math.sqrt(2(N ** 2))
	return (center, radius)

	(x – h)2 + (y – k)2 = r2

for x in range(1, 10**11)